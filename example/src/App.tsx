import React from 'react';
import {ALARM_CLOCK} from 'use-constant-emoji'

function App() {
  return (
    <div className="App">
      <span>{ALARM_CLOCK}</span>
    </div>
  );
}

export default App;
